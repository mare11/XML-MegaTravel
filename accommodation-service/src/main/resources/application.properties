server.port=8081
spring.application.name=accommodation-service

# spring.datasource properties go instead of this one
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration